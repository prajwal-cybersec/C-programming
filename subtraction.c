#include <stdio.h>

int main() {
    int a, b, sub;

    printf("Enter first number: ");
    scanf("%d", &a);

    printf("Enter second number: ");
    scanf("%d", &b);

    sum = a - b;

    printf("Sub = %d", sub);

    return 0;
}
